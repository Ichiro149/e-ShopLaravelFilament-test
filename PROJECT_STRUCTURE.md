# 📁 Структура проекта e-Shop

## Основные папки

### `/app` - Логика приложения
```
app/
├── Console/Commands/     # Artisan команды
├── Filament/            # Админ-панели (Filament)
│   ├── Resources/       # Ресурсы админ-панели
│   └── Seller/          # Панель продавца
├── Helpers/             # Вспомогательные функции
├── Http/
│   ├── Controllers/     # Контроллеры (обработка запросов)
│   ├── Middleware/      # Промежуточное ПО (авторизация, баны)
│   └── Requests/        # Валидация форм
├── Jobs/                # Фоновые задачи
├── Livewire/            # Интерактивные компоненты
├── Models/              # Модели данных (36 шт)
├── Notifications/       # Email/Push уведомления
├── Observers/           # Обработчики событий моделей
├── Policies/            # Политики доступа
├── Providers/           # Сервис-провайдеры
├── Services/            # Бизнес-логика
└── Traits/              # Переиспользуемый код
```

### `/resources` - Фронтенд
```
resources/
├── css/                 # Стили (CSS)
│   └── settings/        # Стили страницы настроек
├── js/                  # JavaScript
├── lang/                # Переводы (en, ru, lv)
└── views/               # HTML шаблоны
    ├── auth/            # Авторизация, регистрация
    ├── cart/            # Корзина
    ├── checkout/        # Оформление заказа
    ├── components/      # UI компоненты
    ├── filament/        # Кастомные страницы админки
    ├── layouts/         # Общие шаблоны
    ├── orders/          # Заказы
    ├── pages/           # Статичные страницы
    ├── products/        # Каталог товаров
    ├── profile/         # Профиль пользователя
    ├── settings/        # Настройки аккаунта
    └── tickets/         # Тикеты поддержки
```

### `/database` - База данных
```
database/
├── factories/           # Фабрики для тестов
├── migrations/          # Миграции (создание таблиц)
└── seeders/             # Заполнение тестовыми данными
```

### `/routes` - Маршруты
```
routes/
├── web.php              # Веб-маршруты (основной файл)
└── console.php          # Консольные команды
```

---

## Ключевые файлы

| Файл | Описание |
|------|----------|
| `routes/web.php` | Все URL маршруты сайта |
| `app/Models/User.php` | Модель пользователя |
| `app/Models/Product.php` | Модель товара |
| `app/Models/Order.php` | Модель заказа |
| `app/Http/Controllers/ProductController.php` | Контроллер товаров |
| `app/Http/Controllers/CartController.php` | Контроллер корзины |
| `app/Http/Controllers/CheckoutController.php` | Контроллер оформления заказа |
| `resources/views/welcome.blade.php` | Главная страница |
| `resources/views/layouts/app.blade.php` | Основной шаблон |

---

## Админ-панели (Filament)

### Админ-панель (`/admin`)
- Управление пользователями, товарами, заказами
- Модерация отзывов и тикетов
- Статистика и аналитика

### Панель продавца (`/seller`)
- Управление своими товарами
- Просмотр заказов на свои товары
- Чат с покупателями

---

## База данных - основные таблицы

| Таблица | Описание |
|---------|----------|
| `users` | Пользователи |
| `products` | Товары |
| `categories` | Категории товаров |
| `orders` | Заказы |
| `order_items` | Товары в заказах |
| `cart_items` | Корзина |
| `companies` | Компании продавцов |
| `reviews` | Отзывы |
| `tickets` | Тикеты поддержки |
| `coupons` | Купоны скидок |
| `bans` | Баны пользователей |

---

## Технологии

- **Backend:** Laravel 12, PHP 8.3
- **Admin Panel:** Filament 3.2
- **Frontend:** Blade, Tailwind CSS, Alpine.js
- **Database:** MySQL
- **Auth:** Laravel Breeze + Socialite (Google, Discord)
- **Realtime:** Livewire
